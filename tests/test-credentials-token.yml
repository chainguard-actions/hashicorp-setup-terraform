name: Test credentials token input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          terraform_version: 'latest'
          cli_config_credentials_hostname: 'app.terraform.io'
          cli_config_credentials_token: 'fake-token-for-testing'
          terraform_wrapper: 'false'
      - name: Assert terraform is installed
        run: |
          echo "Checking terraform binary is available..."
          if ! command -v terraform >/dev/null 2>&1; then
            echo "FAIL: terraform binary not found on PATH"
            exit 1
          fi
          VERSION=$(terraform version -json 2>/dev/null | grep -o '"terraform_version":"[^"]*"' | head -1 || terraform version | head -1)
          echo "Terraform version: $VERSION"
          echo "PASS: terraform is installed"
      - name: Assert credentials config was written
        run: |
          echo "Checking Terraform CLI config for credentials block..."
          # Terraform CLI config is typically at ~/.terraformrc or $TF_CLI_CONFIG_FILE
          CONFIG_FILE="${TF_CLI_CONFIG_FILE:-$HOME/.terraformrc}"
          echo "Config file path: $CONFIG_FILE"
          if [ -f "$CONFIG_FILE" ]; then
            echo "Config file exists"
            # Check that credentials block is present for the hostname
            if grep -q 'credentials' "$CONFIG_FILE"; then
              echo "PASS: credentials block found in Terraform CLI config"
            else
              echo "FAIL: credentials block not found in Terraform CLI config"
              cat "$CONFIG_FILE"
              exit 1
            fi
          else
            echo "FAIL: Terraform CLI config file not found at $CONFIG_FILE"
            exit 1
          fi
